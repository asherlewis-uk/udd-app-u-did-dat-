# Environment Contract

Back to [docs/\_INDEX.md](./_INDEX.md).

## How to read this file

- This is the canonical env and config catalog for the repo.
- It includes both typed config from `packages/config` and raw `process.env` reads still present in apps and packages.
- If a variable is used in code or workflows and not listed here, that is a docs bug.
- If config behavior disagrees with code behavior, the drift is called out explicitly and also tracked in [docs/implementation-gaps.md](./implementation-gaps.md).
- **Infrastructure commitment (ADR 015):** This contract is committed strictly to GCP Cloud Run, Secret Manager, GCS, and Pub/Sub. All AWS/SQS references have been removed. See [ADR 015](./adr/015-canonical-hosted-baseline-and-middleware.md).

## Core runtime variables

| Variable            | Owner                                      | Required                               | Default       | Allowed values                                 | Effect                                                                                                                                                                                                       | Sensitivity | Scope               |
| ------------------- | ------------------------------------------ | -------------------------------------- | ------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ------------------- |
| `NODE_ENV`          | All services, especially logger            | Optional in code, required in practice | `development` | `development`, `staging`, `production`, `test` | Controls environment mode, non-prod stack traces, and logger behavior. No longer used for secret-manager selection; see `SECRET_MANAGER_PROVIDER`.                                                           | Low         | local, runtime, ops |
| `DATABASE_URL`      | `packages/database` and all DB-backed apps | Yes                                    | None          | PostgreSQL URI                                 | Connects services to PostgreSQL                                                                                                                                                                              | High        | local, runtime, ops |
| `DATABASE_POOL_MAX` | `packages/config`                          | No                                     | `10`          | Integer                                        | Sets DB pool max                                                                                                                                                                                             | Low         | runtime, ops        |
| `DATABASE_SSL`      | `packages/config`                          | No                                     | `false`       | `true`, `false`, `1`, `0`                      | Enables DB SSL at config layer                                                                                                                                                                               | Low         | runtime, ops        |
| `REDIS_URL`         | `packages/config`                          | Yes                                    | None          | Redis URI                                      | Connects services to Redis                                                                                                                                                                                   | Medium      | local, runtime, ops |
| `PORT`              | `packages/config`, app entrypoints         | No                                     | App-specific  | Integer                                        | Listener port for each Node service; read via `config.port(fallback)`. Defaults: api=8080, gateway=3000, orchestrator=3002, collaboration=3003, ai-orchestration=8080, worker-manager=3005, usage-meter=3006, web=3007 | Low         | local, runtime      |

## Auth and session variables

| Variable                       | Owner                | Required                               | Default | Allowed values        | Effect                                                              | Sensitivity | Scope               |
| ------------------------------ | -------------------- | -------------------------------------- | ------- | --------------------- | ------------------------------------------------------------------- | ----------- | ------------------- |
| `JWT_SECRET`                   | `packages/auth`, API | Yes                                    | None    | Any non-empty secret  | Signs and validates session JWTs                                    | High        | local, runtime, ops |
| `JWT_EXPIRES_IN_SECONDS`       | `packages/config`    | No                                     | `86400` | Integer seconds       | Session token TTL                                                   | Low         | runtime, ops        |
| `WORKOS_API_KEY`               | `WorkOSAuthProvider` | Yes for auth flows                     | None    | WorkOS API key        | Server-to-WorkOS auth calls                                         | High        | local, runtime, ops |
| `WORKOS_CLIENT_ID`             | Auth flows           | Yes for auth flows                     | None    | WorkOS client ID      | Used by server-side auth wiring and iOS config inputs               | Low         | local, runtime, ops |
| `WORKOS_WEBHOOK_SECRET`        | Auth flows           | Yes where webhook verification is used | None    | WorkOS webhook secret | Validates incoming WorkOS webhook traffic                           | High        | runtime, ops        |
| `NEXT_PUBLIC_WORKOS_CLIENT_ID` | `apps/web`           | Yes for hosted web sign-in             | None    | WorkOS client ID      | Used by the hosted web login page to construct WorkOS auth requests | Low         | local, runtime      |

## Service discovery and client URLs

| Variable                    | Owner                           | Required                           | Default                           | Allowed values | Effect                                                                                                                 | Sensitivity | Scope          |
| --------------------------- | ------------------------------- | ---------------------------------- | --------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------- | ----------- | -------------- |
| `API_BASE_URL`              | Internal callers                | **Yes in production**, No in dev   | `http://localhost:8080` (dev only) | URL            | Base URL other services use for API calls. **Required in production** — throws at startup if unset when `NODE_ENV=production`. Note: API service code itself defaults to port `8080` unless `PORT` is overridden. No current server-side consumer; wire in Terraform when a consumer is added. | Low         | local, runtime |
| `ORCHESTRATOR_BASE_URL`     | Internal callers                | **Yes in production**, No in dev   | `http://localhost:3002` (dev only) | URL            | Base URL for orchestrator. **Required in production** — throws at startup if unset when `NODE_ENV=production`. No current consumer; wire in Terraform when a consumer is added. | Low         | local, runtime |
| `COLLABORATION_BASE_URL`    | Internal callers                | **Yes in production**, No in dev   | `http://localhost:3003` (dev only) | URL            | Base URL for collaboration service (dormant). **Required in production** — throws at startup if unset when `NODE_ENV=production`. No current consumer; wire in Terraform when a consumer is added. | Low         | local, runtime |
| `AI_ORCHESTRATION_BASE_URL` | Internal callers                | **Yes in production**, No in dev   | `http://localhost:8080` (dev only) | URL            | Base URL for AI orchestration service. **Required in production** — throws at startup if unset when `NODE_ENV=production`. Local dev falls back to `http://localhost:8080`. Terraform wires this automatically from the Cloud Run service URL. **Canonical name** — the legacy alias `AI_ORCHESTRATION_URL` has been removed; use this variable exclusively. | Low         | local, runtime |
| `WORKER_MANAGER_BASE_URL`   | Host agent and internal callers | **Yes in production**, No in dev   | `http://localhost:3005` (dev only) | URL            | Base URL for worker-manager capacity snapshot ingestion. **Required in production** — throws at startup if unset when `NODE_ENV=production`. Consumed by host-agent (not a Cloud Run service). Production value available as Terraform output `worker_manager_service_url`; must be set in host-agent deployment config. | Low         | local, runtime |
| `GATEWAY_BASE_URL`          | Internal callers                | **Yes in production**, No in dev   | `http://localhost:3000` (dev only) | URL            | Base URL for hosted preview gateway. **Required in production** — throws at startup if unset when `NODE_ENV=production`. No current consumer; wire in Terraform when a consumer is added. | Low         | local, runtime |
| `NEXT_PUBLIC_API_URL`       | `apps/web`                      | Yes for useful local web work      | None                              | URL            | Browser-visible API base URL                                                                                           | Low         | local, runtime |
| `GATEWAY_URL`               | `apps/web`                      | Yes for preview links and rewrites | None                              | URL            | Browser-visible gateway URL                                                                                            | Low         | local, runtime |

## Hosted runtime, preview, and AI execution

| Variable                        | Owner                                  | Required | Default          | Allowed values    | Effect                                                                                                                                                                                                                           | Sensitivity | Scope          |
| ------------------------------- | -------------------------------------- | -------- | ---------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------- |
| `WORKER_HEARTBEAT_INTERVAL_MS`  | `packages/config`, host agent          | No       | `30000`          | Integer ms        | Host-agent heartbeat interval                                                                                                                                                                                                    | Low         | runtime, ops   |
| `WORKER_HOST`                   | `packages/config`, `apps/host-agent`   | No       | `localhost`      | Hostname or IP    | Reported runtime-host identity in heartbeats; read via `config.worker.host()`                                                                                                                                                    | Low         | local, runtime |
| `WORKER_SUBNET_PREFIX`          | `packages/config`, `apps/gateway`      | No       | `10.`            | Prefix string     | Gateway allowlist for runtime target addresses; read via `config.gateway.workerSubnetPrefix()`                                                                                                                                   | Low         | runtime, ops   |
| `SANDBOX_LEASE_TTL_SECONDS`     | `packages/config`, `apps/orchestrator` | No       | `86400`          | Integer seconds   | Lease expiry window for runtime allocation; read via `config.runtime.sandboxLeaseTtlSeconds()`                                                                                                                                   | Low         | runtime, ops   |
| `PREVIEW_DOMAIN`                | `packages/config`                      | No       | `localhost:3000` | Host or host:port | Default preview host construction                                                                                                                                                                                                | Low         | local, runtime |
| `PREVIEW_DEFAULT_TTL_SECONDS`   | `packages/config`                      | No       | `3600`           | Integer seconds   | Default preview TTL                                                                                                                                                                                                              | Low         | runtime, ops   |
| `IDLE_THRESHOLD_SECONDS`        | `packages/config`                      | No       | `1800`           | Integer seconds   | Idle-session reap threshold; read via `config.runtime.idleThresholdSeconds()`                                                                                                                                                    | Low         | runtime, ops   |
| `SCAN_INTERVAL_MS`              | `packages/config`                      | No       | `60000`          | Integer ms        | Session-reaper scan loop interval; read via `config.runtime.scanIntervalMs()`. **Note:** When session-reaper is refactored to a scheduled job, this variable becomes a no-op — the scheduler cadence replaces the scan interval. | Low         | runtime, ops   |
| `IDLE_SESSION_SCAN_INTERVAL_MS` | `packages/config`                      | No       | `60000`          | Integer ms        | Typed config accessor for a scan interval, but current session-reaper code does not use this name directly. **Note:** Becomes a no-op under the scheduled-job model.                                                             | Low         | runtime, ops   |
| `STUCK_RUN_TIMEOUT_MS`          | `packages/config`                      | No       | `300000`         | Integer ms        | AI run timeout threshold for stuck-run handling                                                                                                                                                                                  | Low         | runtime, ops   |
| `PIPELINE_MAX_NODES`            | `packages/config`                      | No       | `500`            | Positive integer  | Maximum nodes accepted by DAG validator; read via `config.runtime.pipelineMaxNodes()`                                                                                                                                            | Low         | runtime, ops   |
| `PIPELINE_MAX_EDGES`            | `packages/config`                      | No       | `5000`           | Positive integer  | Maximum edges accepted by DAG validator; read via `config.runtime.pipelineMaxEdges()`                                                                                                                                            | Low         | runtime, ops   |

## Secret manager, storage, and queue variables

| Variable                  | Owner                                  | Required                                    | Default      | Allowed values          | Effect                                                                                                                                                                                                                                                                                                                 | Sensitivity | Scope               |
| ------------------------- | -------------------------------------- | ------------------------------------------- | ------------ | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------- |
| `SECRET_MANAGER_PROVIDER` | `packages/config`                      | No                                          | `gcp`        | `gcp`, `memory`         | Sole selection authority for secret-manager provider. All services read from this config flag via `config.secrets.provider()`. Default is `gcp` (`GCPSecretManagerProvider`). Set to `memory` for local development (`InMemorySecretManagerProvider`). AWS support has been removed per [ADR 015](./adr/015-canonical-hosted-baseline-and-middleware.md). | Low         | runtime, ops        |
| `OBJECT_STORAGE_PROVIDER` | `packages/config`                      | No                                          | `gcs`        | `gcs`, `local`          | Sole selection authority for object-storage adapter family. All services read from this config flag via `config.storage.provider()`. Default is `gcs` (`GCSStorageProvider`, GCP Cloud Storage). Set to `local` for local development. AWS support has been removed per [ADR 015](./adr/015-canonical-hosted-baseline-and-middleware.md).                 | Low         | local, runtime, ops |
| `OBJECT_STORAGE_BUCKET`   | `packages/config`                      | Yes when storage is used                    | None         | Bucket name             | Bucket or local-bucket identifier for object storage                                                                                                                                                                                                                                                                   | Medium      | local, runtime, ops |
| `QUEUE_PROVIDER`          | `packages/config`                      | No                                          | `pubsub`     | `pubsub`, `noop`        | Sole selection authority for queue provider. All services read from this config flag via `config.queue.provider()`. Default is `pubsub` (`PubSubEventPublisher`, GCP Cloud Pub/Sub). Set to `noop` for local development (`NoopEventPublisher`). SQS support has been removed per [ADR 015](./adr/015-canonical-hosted-baseline-and-middleware.md).       | Low         | local, runtime, ops |
| `GCP_PROJECT_ID`          | `packages/events`, infra and workflows | Required for Pub/Sub and hosted deployments | None         | GCP project ID          | Required by `PubSubEventPublisher` and many hosted deployment paths                                                                                                                                                                                                                                                    | Medium      | runtime, ops        |
| `PUBSUB_TOPIC_PREFIX`     | `packages/events`                      | No                                          | `udd`        | String prefix           | Prefix for Pub/Sub topic names                                                                                                                                                                                                                                                                                         | Low         | runtime, ops        |

## Realtime middleware variables (Pusher)

Per [ADR 015](./adr/015-canonical-hosted-baseline-and-middleware.md), Pusher is the canonical realtime middleware for state synchronization and collaboration.

| Variable          | Owner                | Required                                 | Default | Allowed values  | Effect                                        | Sensitivity | Scope               |
| ----------------- | -------------------- | ---------------------------------------- | ------- | --------------- | --------------------------------------------- | ----------- | ------------------- |
| `PUSHER_APP_ID`   | `apps/collaboration` | Yes when collaboration service is active | None    | Pusher app ID   | Identifies the Pusher application             | Medium      | runtime, ops        |
| `PUSHER_KEY`      | `apps/collaboration` | Yes when collaboration service is active | None    | Pusher key      | Client-facing Pusher key for channel auth     | Low         | runtime, ops        |
| `PUSHER_SECRET`   | `apps/collaboration` | Yes when collaboration service is active | None    | Pusher secret   | Server-side Pusher secret for event publishing | High        | runtime, ops        |
| `PUSHER_CLUSTER`  | `apps/collaboration` | Yes when collaboration service is active | None    | Pusher cluster  | Pusher cluster region (e.g. `us2`, `eu`)      | Low         | runtime, ops        |

## Observability variables

| Variable            | Owner                     | Required | Default                                                | Allowed values                   | Effect                                         | Sensitivity | Scope               |
| ------------------- | ------------------------- | -------- | ------------------------------------------------------ | -------------------------------- | ---------------------------------------------- | ----------- | ------------------- |
| `OTLP_ENDPOINT`     | `packages/config`         | No       | Empty string                                           | URL                              | Collector endpoint for traces or logs if wired | Low         | runtime, ops        |
| `OTEL_SERVICE_NAME` | `packages/config`, logger | No       | `udd-service` in config, `udd` in root logger fallback | String                           | Service label in telemetry and logs            | Low         | runtime, ops        |
| `LOG_LEVEL`         | logger                    | No       | `info`                                                 | `debug`, `info`, `warn`, `error` | Minimum log level                              | Low         | local, runtime, ops |

## Workspace-shaped legacy and drift-prone variables

| Variable                  | Owner                               | Required                                                            | Default | Allowed values | Effect                                                                          | Sensitivity | Scope               |
| ------------------------- | ----------------------------------- | ------------------------------------------------------------------- | ------- | -------------- | ------------------------------------------------------------------------------- | ----------- | ------------------- |
| `DEFAULT_ORGANIZATION_ID` | `apps/api/src/routes/workspaces.ts` | No, but required if creating workspaces without request body org ID | None    | UUID           | Fallback organization ID for workspace creation in current workspace-shaped API | Low         | local, runtime, ops |

## Workflow-only GitHub secrets and automation inputs

These are not authoritative architecture inputs, but they are relevant because the current repo automation reads them.

| Variable                         | Owner                                             | Required                 | Default | Allowed values                 | Effect                                                                      | Sensitivity | Scope |
| -------------------------------- | ------------------------------------------------- | ------------------------ | ------- | ------------------------------ | --------------------------------------------------------------------------- | ----------- | ----- |
| `GCP_WORKLOAD_IDENTITY_PROVIDER` | `.github/workflows/deploy.yml`, `terraform.yml`   | Yes for current workflow | None    | GCP workload identity provider | Used for GitHub-to-GCP auth in deploy and terraform workflows               | Medium      | ops   |
| `GCP_CI_SERVICE_ACCOUNT`         | `.github/workflows/deploy.yml`, `terraform.yml`   | Yes for current workflow | None    | GCP service account email      | Used for GitHub-to-GCP auth in deploy and terraform workflows               | Medium      | ops   |
| `GCP_PROJECT_ID`                 | `.github/workflows/deploy.yml`, `terraform.yml`   | Yes for current workflow | None    | GCP project ID                 | Used to tag/push images and passed into Terraform plan as `TF_VAR_project_id` | Medium      | ops   |

## iOS compile-time configuration

The iOS client uses Xcode build-phase parameterization via `Info.plist` keys backed by build settings. `AppConfig.swift` reads from `Bundle.main.infoDictionary`. DEBUG / local-dev builds may fall back to local defaults when keys are absent or unresolved; non-debug builds fail fast instead of silently targeting localhost.

| Build Setting          | Info.plist Key         | Required for hosted | Hardened-v1 Release default   | Local-dev fallback      | Effect                                              |
| ---------------------- | ---------------------- | ------------------- | ----------------------------- | ----------------------- | --------------------------------------------------- |
| `UDD_API_BASE_URL`     | `UDD_API_BASE_URL`     | Yes                 | `https://api.asherlewis.org`  | `http://localhost:8080` | API base URL for all iOS API calls                  |
| `UDD_GATEWAY_BASE_URL` | `UDD_GATEWAY_BASE_URL` | Yes                 | `https://api.asherlewis.org`  | `http://localhost:3000` | Gateway base URL for preview routing (preview is path-routed under `/preview/*` behind the API host) |
| `UDD_WORKOS_CLIENT_ID` | `UDD_WORKOS_CLIENT_ID` | Yes for auth flows  | Not baked; set per build      | Empty string (DEBUG only) | WorkOS client ID for PKCE auth                      |

`UDDCompanion.xcodeproj` ships the Release build config with `UDD_API_BASE_URL` and `UDD_GATEWAY_BASE_URL` set to the hardened-v1 canonical host. Debug is intentionally left blank so local-dev fallbacks remain unchanged. Override in an `.xcconfig` or target build setting if a non-canonical environment is needed. SPM-only DEBUG builds without Xcode build-setting overrides use local-dev fallbacks automatically.

## Hardened-v1 canonical public hosts

These are the canonical public hostnames for the hosted product. The load-balancer and DNS cutover runbook lives at [runbooks/domain-cutover.md](./runbooks/domain-cutover.md).

| Host                         | Role                                          | Backend                                                                                                                  | Status                                                                                      |
| ---------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| `api.asherlewis.org`         | Public API + preview gateway                  | GCP global HTTPS LB → serverless NEGs → `apps/api` (default) + `apps/gateway` (`/preview/*`)                             | Repo-ready. LB, URL map, and managed cert already defined in `infra/terraform`. DNS pending. |
| `app.asherlewis.org`         | Primary web origin (Next.js)                  | No deployment target exists yet (see [implementation-gaps.md](./implementation-gaps.md))                                 | Reserved. DNS intentionally unpointed until a web host (Cloud Run service, Cloudflare Pages, etc.) is chosen. |
| `www.asherlewis.org`         | 301 redirect to `app.asherlewis.org`          | Cloudflare Bulk Redirect / Page Rule (no origin)                                                                         | Reserved. Wire once `app.asherlewis.org` resolves.                                          |

### Browser-side host behaviour

- `apps/web` expects `/v1/*` and `/preview/*` to be same-origin to the web host. `next.config.js` rewrites both to `API_BASE_URL` and `GATEWAY_URL` (server-side env vars). Keeping API traffic same-origin avoids cross-origin calls — `apps/api` has no CORS middleware.
- `apps/mobile-ios` calls `https://api.asherlewis.org/v1/*` and `https://api.asherlewis.org/preview/*` directly.
- WorkOS redirect URIs: web uses `https://app.asherlewis.org/auth/callback` (dynamic `window.location.origin + /auth/callback`); iOS uses `uddcompanion://auth/callback` (PKCE with `ASWebAuthenticationSession`). Both must be registered in the WorkOS dashboard.

### Staging counterpart

For symmetry, staging uses `staging-api.asherlewis.org` (API host) and reserves `staging-app.asherlewis.org` (web origin, not yet wired). The prod-domain commitment is the canonical hardened-v1 surface; staging is operational scaffolding.
