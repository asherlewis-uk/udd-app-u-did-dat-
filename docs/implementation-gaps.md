# Implementation Gaps

Back to [docs/\_INDEX.md](./_INDEX.md).

## How to use this file

- This file is the mandatory register for any meaningful doc or architecture drift.
- If a file conflicts with [AGENTS.md](../AGENTS.md) or the canonical-doc priority in [docs/\_INDEX.md](./_INDEX.md), treat that file as stale and record the conflict here.
- Do not smooth over gaps by rewriting canonical docs to match accidental implementation details.

## Gap table

| Desired source of truth                                           | Current code reality                                                                                                                                                                | Gap                                                                                         | User impact                                                                   | Severity | Decision status                                                                                                                                                                                                                             | Owner or next step                                                                                         |
| ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Solo-first, project-centered domain model                         | Schema, auth, API routes, web UI, and iOS API all use `Organization -> Workspace -> Project`                                                                                        | Canonical product model and implementation model diverge                                    | High cognitive load, stale terminology, harder future simplification          | High     | **Decision made.** Thin-workspace migration: `workspaceId` stays as an internal shard key, but is removed from the product API surface, client UIs, and canonical contracts. See [ADR 013](./adr/013-thin-workspace-migration-strategy.md). | Implementation open. Migrate routes, clients, and auth claims to project-first shapes.                     |
| Hosted-first product story                                        | Older docs assumed local-first in several places before this reset                                                                                                                  | Canonical hosted-first story required a full doc rewrite                                    | Confusing onboarding and architecture assumptions                             | Medium   | **No longer an open decision.** Canonical docs are now hosted-first.                                                                                                                                                                        | Keep hosted-first docs authoritative; archive stale planning after replacements exist.                     |
| Web and iOS are first-class client surfaces                       | Web is primary in code; iOS exists but still uses workspace-shaped APIs and hardcoded config                                                                                        | First-class status exists in product scope, but iOS is not yet equally operationally mature | Risk of web-only changes breaking iOS and vice versa                          | High     | **Decision made.** Shared contracts (`packages/contracts`) plus iOS conformance tests enforce parity. See quality gates.                                                                                                                    | Implementation open. Add iOS conformance test suite.                                                       |
| iOS must be first-class without downgrading web                   | No code path currently demotes web, but nothing in old docs prevented it                                                                                                            | Needed an explicit rule in canonical docs and ADRs                                          | Future planning could silently skew to one client surface                     | Medium   | **No longer an open decision.** ADR 012 and constraints doc enforce this rule.                                                                                                                                                              | Keep the no-web-downgrade rule in constraints, ADRs, and quality gates.                                    |
| Hosted runtime is canonical and production-ready                  | Host-agent capacity reporting is stubbed and no real container-per-session isolation exists                                                                                         | Runtime lifecycle exists, but isolation and capacity truth are incomplete                   | Session startup and safety assumptions are weaker than canonical docs require | High     | **Decision made.** Container-per-session on a managed container platform. See [ADR 014](./adr/014-container-per-session-isolation.md).                                                                                                      | Implementation open. Implement container-per-session isolation, real capacity reporting, and sandbox boot. |
| Worker-manager is a hosted runtime service                        | `apps/worker-manager` is an HTTP service, but Terraform models it as a Cloud Run Job                                                                                                | Infra and code disagree on service shape                                                    | Hosted runtime cannot be reasoned about cleanly from infra alone              | High     | **Decision made.** Worker-manager is a service. Infra must be corrected to match code and service-catalog.                                                                                                                                  | Implementation open. Fix Terraform resource type.                                                          |
| Session reaper should be a scheduled cleanup job                  | Code is a long-running interval loop; infra schedules it as a job                                                                                                                   | Execution model is split between service-style and job-style assumptions                    | Operators can misdiagnose stuck cleanup behavior                              | High     | **Decision made.** Session reaper becomes a scheduled single-invocation job. Code refactored to process-once-and-exit; infra uses a scheduler trigger.                                                                                      | Implementation open. Refactor code entry point and align infra.                                            |
| Deploy workflow should match runtime topology                     | `.github/workflows/deploy.yml` deploys Cloud Run services for components Terraform models as jobs                                                                                   | Workflow and infra disagree                                                                 | Release behavior can diverge from intended runtime shape                      | High     | **Narrowed.** Worker-manager is a service and session-reaper is a job. Workflows must be updated to match these classifications.                                                                                                            | Implementation open. Fix workflows after infra decisions are applied.                                      |
| Terraform workflow should reflect repo infrastructure             | `.github/workflows/terraform.yml` uses AWS credentials while Terraform is GCP-oriented                                                                                              | Workflow source of truth is stale                                                           | Misleading CI, broken plans, operator confusion                               | High     | **Still open.** Direction is a single coherent hosted deployment story, but the specific credential and auth model change has not been applied.                                                                                             | Implementation open. Fix workflow auth model to match hosted infra.                                        |
| Build and deploy registries should agree                          | `.github/workflows/build.yml` assumes GHCR while deploy uses GCR                                                                                                                    | Container publishing story is inconsistent                                                  | Build artifacts and deployment assumptions diverge                            | Medium   | **Still open.** Direction is one coherent registry story but the specific fix has not been applied.                                                                                                                                         | Implementation open. Align registries.                                                                     |
| Environment contract should be authoritative                      | `packages/config` covers some env vars, but services still read raw `process.env` for `PORT`, `IDLE_THRESHOLD_SECONDS`, `SCAN_INTERVAL_MS`, `SANDBOX_LEASE_TTL_SECONDS`, and others | Config contract is incomplete                                                               | Harder local setup, weaker operator confidence, hidden behavior               | High     | **Decision made.** `packages/config` becomes the single typed source of truth. All raw `process.env` reads are migrated behind config accessors.                                                                                            | Implementation open. Add typed accessors and migrate apps.                                                 |
| Secret-manager selection should follow a single explicit contract | Current code uses `NODE_ENV === 'production'` in `apps/ai-orchestration/src/context.ts`, while `packages/config` exposes `SECRET_MANAGER_PROVIDER` with AWS-oriented defaults       | Secret-manager source of truth is split                                                     | High risk of operator misunderstanding                                        | High     | **Decision made.** One explicit config flag (`SECRET_MANAGER_PROVIDER`) is the sole selection authority. `NODE_ENV`-based selection is removed.                                                                                             | Implementation open. Update ai-orchestration to read from config.                                          |
| Polyglot support should have a first-class stack boundary         | Repo has no dedicated stack registry, detector, or adapter catalog                                                                                                                  | Polyglot story is real in product scope but not yet first-class in implementation           | AI and scaffold behavior cannot be reliably stack-aware                       | High     | **Decision made.** Static config-driven stack registry with declarative stack definitions.                                                                                                                                                  | Implementation open. Create stack-registry package.                                                        |
| New project creation should pass through a scaffold engine        | No first-class scaffold/template engine exists in runtime code                                                                                                                      | Idea-to-project flow is incomplete                                                          | Product cannot yet fully deliver the canonical create flow                    | High     | **Decision made.** Built-in template engine with bundled, repo-versioned templates.                                                                                                                                                         | Implementation open. Create scaffold package and templates directory.                                      |
| AI edits should be able to use project indexing or memory         | No dedicated indexing or memory subsystem exists                                                                                                                                    | AI architecture is missing a key retrieval boundary                                         | AI editing context is weaker than canonical docs expect                       | Medium   | **Deferred.** Implementation blocked until stack registry exists. In the interim, AI edits use explicit file and project context only.                                                                                                      | Dependency-blocked by stack registry.                                                                      |
| Hosted preview should be stable and secure                        | Gateway preview path exists, but runtime isolation and route lifecycle still depend on incomplete runtime plumbing                                                                  | Preview story is implemented only partially end-to-end                                      | Users may see preview failures that look like product bugs                    | High     | **Narrowed.** Hosted preview is canonical (ADR 011). Remaining gap is that preview completeness depends on runtime isolation (ADR 014).                                                                                                     | Implementation open. Preview becomes end-to-end once container-per-session isolation is in place.          |
| Local development should be a supported path                      | `pnpm dev` starts multiple apps with overlapping default ports (`web`, `gateway`, `usage-meter`)                                                                                    | Local dev entrypoint is not clean                                                           | Friction for contributors and operators                                       | Medium   | **Decision made.** Code defaults are changed so no two services share a default port.                                                                                                                                                       | Implementation open. Change port defaults in colliding services.                                           |
| First-class iOS surface should be easy to configure               | `apps/mobile-ios/Sources/App/AppConfig.swift` hardcodes local and production URLs and contains a placeholder WorkOS client ID                                                       | iOS local and production configuration are not fully productized                            | High chance of broken local iOS validation                                    | High     | **Decision made.** Placeholder values replaced with real environment-specific compile-time config using standard Xcode mechanisms.                                                                                                          | Implementation open. Update AppConfig.swift with real values and build-phase parameterization.             |
| Solo-first product should not make collaboration a product center | `apps/collaboration` and related APIs exist                                                                                                                                         | Collaboration support exists, but it should remain optional in product docs                 | Scope creep and wrong architectural emphasis                                  | Medium   | **Decision made.** Collaboration is frozen as dormant. No new feature work. Not removed from repo. Not promoted to core.                                                                                                                    | No implementation action needed beyond status marker.                                                      |
| Observability should be operational, not aspirational             | Metrics exporter is noop by default; traces are lightweight correlation only; no dashboards or alert rules exist                                                                    | Observability is stronger in intent than implementation                                     | Low operator confidence for hosted mode diagnostics                           | Medium   | **Decision made.** Log-based alerts for critical conditions first. Full metrics export follows as a second step.                                                                                                                            | Implementation open. Create log-based alert rules for documented critical conditions.                      |
| Queue provider default should match hosted infrastructure         | `packages/config` defaults to `sqs`; `packages/events` defaults to `noop` when unset; hosted infra implies a different queue provider                                               | Three conflicting defaults across config, events, and hosted reality                        | Operator confusion on queue setup; silent noop in hosted mode if unset        | Medium   | **Decision made.** Config-layer default for `QUEUE_PROVIDER` aligned to the actual hosted infrastructure backend.                                                                                                                           | Implementation open. Fix config default and ensure events package reads from config.                       |

## Stale file register

| Stale file                          | Conflict                                                                             | Winning source                                                                                                                                                                                                                                  |
| ----------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/adr/003-workspace-tenancy.md` | Records the old workspace-owned canonical model                                      | [docs/domain-model.md](./domain-model.md), [docs/adr/010-project-centered-identity-model.md](./adr/010-project-centered-identity-model.md), [docs/adr/013-thin-workspace-migration-strategy.md](./adr/013-thin-workspace-migration-strategy.md) |
| `docs/adr/004-microvm-isolation.md` | Records a stronger runtime-isolation implementation than the repo currently provides | [docs/runtime.md](./runtime.md), [docs/adr/008-hosted-execution-canonical.md](./adr/008-hosted-execution-canonical.md), [docs/adr/014-container-per-session-isolation.md](./adr/014-container-per-session-isolation.md)                         |
| `.github/workflows/terraform.yml`   | Encodes AWS secrets for a GCP-oriented Terraform tree                                | [docs/architecture.md](./architecture.md), [docs/ENV_CONTRACT.md](./ENV_CONTRACT.md)                                                                                                                                                            |
| `.github/workflows/build.yml`       | Encodes GHCR assumptions while deploy uses GCR                                       | [docs/architecture.md](./architecture.md), this gap register                                                                                                                                                                                    |
| `.github/workflows/deploy.yml`      | Deploys some components as services while Terraform models them as jobs              | [docs/architecture.md](./architecture.md), [docs/service-catalog.md](./service-catalog.md)                                                                                                                                                      |

## Notes

- This file is intentionally blunt.
- Canonical docs describe the product and architecture we are standardizing around now.
- The codebase still carries older workspace-centric and partially incomplete hosted-runtime assumptions. Those remain implementation facts until code changes, but they do not override the canonical docs.
- Two new ADRs were created as part of this decision pass: [ADR 013](./adr/013-thin-workspace-migration-strategy.md) (thin-workspace migration) and [ADR 014](./adr/014-container-per-session-isolation.md) (container-per-session isolation).
- Two new gap rows were added: observability approach and queue provider default.
- Collaboration is frozen as dormant. No gap row removal is needed, but no new feature work should be scoped for it.
